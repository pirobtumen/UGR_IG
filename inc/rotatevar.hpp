#ifndef __ROTATEVAR_HPP
#define __ROTATEVAR_HPP

const double max_rotate = 360;
const double MAX_SPEED = 25;
const double MIN_SPEED = 0;
const double SPEED_INC = 0.5;

extern double rotate_angle;
extern double speed;

#endif
